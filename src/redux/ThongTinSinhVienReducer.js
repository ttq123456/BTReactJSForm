const initialState = {
    SinhVien: {
        maSV:"",
        hoTen:"",
        sdt:"",
        email:""
    }
}

export default (state = initialState, action) => {
  switch (action.type) {

  

  default:
    return state
  }
}
